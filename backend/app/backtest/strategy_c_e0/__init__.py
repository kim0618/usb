"""C-E0: event split of the frozen C-M V1 candidates (declaration `c-e0-event-momentum-v1`).

Nothing here re-tunes C-M. The package reads the frozen declaration and taxonomy, builds a
PIT event view of SEC EDGAR submissions, assigns one status per C-M0 candidate row, and stops
at the pre-outcome checks unless every one of them passes.
"""
