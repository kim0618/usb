"""Strategy D-AGGRESSIVE (MARKET_STRUCTURE_ANALOG_TAIL_V1): tail-capture screening on V2-A's frozen A(q).

A separate package on purpose: V2-A's run identity hashes every ``*.py`` under
``strategy_d_v2/``, so a D-AGG module placed there would move V2-A's run ids.
"""
