"""C-ATTACK-V0: right-tail harvest pre-validation of the frozen C-M0 M_ONLY candidates.

A new study ID with its own declaration (`c_attack_rules_v1.json`). It does not reopen the closed
Strategy C directional family and changes no C rule: it reuses the frozen C-M0 candidate table,
the frozen C-E0 M_ONLY status, and asks whether a fixed take-profit / stop-loss exit turns the
observed right-tail frequency into realized, cost-aware expectancy versus matched controls.
"""
